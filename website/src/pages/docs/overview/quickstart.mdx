# Quickstart

This guide will walk you through the steps to get started with using
`@terai/vite` in your Next.js project.

## Prerequisites

Before you begin, make sure you have the following installed:

- Node.js
- Next.js

## Installation

To install `@terai/vite`, run the following command:
