# `getLocale`

Retrieves the current global locale.

```ts
import { getLocale } from '@terai/node'

const currentLocale = getLocale()
```

## Return Value

Returns the current locale string (e.g., `'en'`).
